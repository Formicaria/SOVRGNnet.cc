{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    "types": ["node", "vite/client"],

    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@shared/*": ["../shared/*"]
    }
  },
  "//": "Deliberately does NOT include ../shared wholesale. shared/types.ts re-exports the drizzle schema, so compiling the whole folder would drag drizzle-orm — a server dependency — into the desktop app. TypeScript follows imports from src/ on its own, which picks up exactly the shared modules this app actually uses.",
  "include": ["src", "vite.config.ts"]
}
