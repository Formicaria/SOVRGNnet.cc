{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "noEmit": true,

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    "types": ["node"],

    "baseUrl": ".",
    "paths": {
      "@shared/*": ["../shared/*"]
    }
  },
  "//": "Like the desktop package, this deliberately doesn't include ../shared wholesale — shared/types.ts re-exports the app's drizzle schema, which has nothing to do with the identity provider. TypeScript follows the imports that are actually used.",
  "include": ["src", "schema.ts", "drizzle.config.ts"]
}
