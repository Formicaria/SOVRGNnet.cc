{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the SOVRGNnet frontend is allowed to ask the shell to do. Deliberately short — anything not listed here is unavailable to the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "deep-link:default",
    "opener:allow-open-url"
  ]
}
